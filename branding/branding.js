/* Orlan Editor branding. The server links this file into every editor page. The server
 * configuration sets the same name; this keeps it when a page loads without that setting. */
window.brandProductName = window.brandProductName || 'Orlan Editor';
window.brandProductURL = window.brandProductURL || 'https://orlan.app';
/* The "Info and support" link of the limit notice. */
window.brandProductFAQURL = 'https://orlan.app';
